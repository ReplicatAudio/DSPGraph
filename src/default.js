export default `// Write your JS code here
// x -> graph x
// s -> sine (Math.sin(x))
// p[index] -> param controls (sliders)
// graph shows last expression 
// or value of 'w[index]'

// Make a square wave
s > 0 ? 1 : -1;
`;